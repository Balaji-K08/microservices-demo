name: build-deploy_pipeline
on: 
  push:
    branches: [main]
  pull_request:
    branches:
      - main
  workflow_dispatch:
  
jobs:
  find-build-list:
    runs-on: ubuntu-slim
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: ls
        run: ls -la
